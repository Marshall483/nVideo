@model ShopCartView

<div class="span9">
  <div class="well well-small">
    <h1>Check Out </h1>
    <hr class="soften" />
    <div class="goods-wrap">
      <table class="table table-bordered table-condensed">
        <thead>
          <tr>
            <th>Product</th>
            <th>Description</th>
            <th>Unit price</th>
            <th>Qty </th>
            <th>Items</th>
          </tr>
        </thead>
        <tbody>
          @{
            if (Model.ShopCartItems != null)
            {
              foreach (var shopCartItem in Model.ShopCartItems)
              {
                <tr>
                  @Html.Partial("ListShopCartPartial", shopCartItem.Entity)
                  <td>
                    <h3><small> @shopCartItem.Quanity.ToString() </small></h3>
                  </td>
                </tr>
              }
            }
            else
            {
              @: Cart is empty
            }
          }
        </tbody>
      </table>
      <hr class="soften" />
      <h3>Total products: @Model.TotalValue</h3>
      @{
        if (Model.ShopCartItems != null)
        {
          <a class="shopBtn btn-large pull-right">Next <span class="icon-arrow-right"></span></a>
        }
      }
    </div>
  </div>
</div>